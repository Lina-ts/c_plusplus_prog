#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <algorithm>
class MNP
{
	
};
/*std::vector<std::vector<string>> read_file()
{
	std::ifstream file("/home/akurbanova/hub_prog/it_school/hometask_1/num.csv");
	std::vector<std::vector<string>> line;
	std::vector<std::string> values;

	while (getline(file, line, ','))
        {
              std::istringstream line_stream(line);
              values.emplace_back(std::istream_iterator<string>(line_stream), std::istream_iterator<string>());

              //values.push_back(line);
        }

        return lines;
}
using namespace std;*/

int main(int argc, char * argv [])
{
	/*std::ifstream file("/home/akurbanova/hub_prog/it_school/hometask_1/num.csv");

	std::vector<std::string> values;
	std::string line;

	while (getline(file, line, ','))
        {
              std::istringstream line_stream(line);
	      values.emplace_back(std::istream_iterator<string>(line_stream), std::istream_iterator<string>());
	      
	      //values.push_back(line);
        }

	return lines;
	read_file();

	for (auto i : values){
		std::cout << i << std::endl;
	}
	std::cout << values.size() << std::endl;*/
	return 0;
}
